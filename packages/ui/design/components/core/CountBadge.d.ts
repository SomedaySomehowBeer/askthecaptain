import type { CSSProperties } from 'react';
/** 44px accent pill holding a single number — the capture queue count. */
export interface CountBadgeProps { count: number | string; style?: CSSProperties }
export declare function CountBadge(props: CountBadgeProps): JSX.Element;
