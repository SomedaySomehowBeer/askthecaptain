One-line: the text field used in sign-in and settings.

```jsx
<Input label="Email" placeholder="you@example.com" type="email" />
<Input label="Repository" defaultValue="someday/brewing" hint="owner/name" invalid />
```

Notes: the field background is cream (`--surface-page`) even inside a white Card — that inset contrast is how inputs read as editable.
