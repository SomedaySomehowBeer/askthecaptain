import type { CSSProperties } from 'react';
/** Hairline rule, optionally with a centred word (the sign-in card's "or"). */
export interface DividerProps { label?: string; style?: CSSProperties }
export declare function Divider(props: DividerProps): JSX.Element;
