One-line: the round accent counter that sits at the right of a queue card.

```jsx
<CountBadge count={3} />
```

Notes: 44px is deliberate — it matches the minimum tap target even though the badge is not tappable. Tabular numerals.
